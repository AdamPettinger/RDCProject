function [M] = createMovie(T, Rl)
% Returns a movie
% The input T is a 4x4x6xn 4D matrix. Each 4x4 slice is a position for one
% of the 6 joints. 'n' is the number of trajectory points

for i=1:length(T)
    
end

end

